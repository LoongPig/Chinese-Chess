#include <bits/stdc++.h>
#include "Solve.hpp"
#include "Board.hpp"
using namespace std;
int main(){
    _setmode(_fileno(stdout), _O_U16TEXT);
    hideCursor();system("mode con cols=42 lines=31");
    Board _const_;
    _const_.print();
    system("pause");
    return 0;
}